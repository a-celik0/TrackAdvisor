@page "{id:int}"
@model TrackAdvisor.WEB.Pages.TopicDetailModel

<div style="padding: 20px;">

    <h1>● @Model.Topic.Name</h1>
    <p>@Model.Topic.Description</p>

    <a href="/ShareExperience?topicId=@Model.Topic.TopicID">
        <button>+ Share experience</button>
    </a>
    <a href="/AskQuestion?topicId=@Model.Topic.TopicID">
        <button>+ Ask a question</button>
    </a>

    <hr />

    <h3>Experiences (@Model.ExperiencePosts.Count)</h3>

    @foreach (var post in Model.ExperiencePosts)
    {
        <div style="border: 1px solid #ccc; padding: 10px; margin: 10px 0;">
            <p>@post.Content</p>
            <small>@post.CreatedAt</small>
        </div>
    }

    <h3>Questions (@Model.Questions.Count)</h3>

    @foreach (var question in Model.Questions)
    {
        <div style="border: 1px solid #ccc; padding: 10px; margin: 10px 0;">
            <p>@question.Content</p>
            <small>@question.CreatedAt</small>

            <!-- O soruya ait cevaplar -->
            @foreach (var answer in Model.Answers.Where(a => a.QuestionID == question.QuestionID))
            {
                <div style="background: #f9f9f9; padding: 8px; margin: 5px 0;">
                    <p>@answer.Content</p>
                    <small>@answer.CreatedAt</small>
                </div>
            }

            <a href="/AddAnswer?questionId=@question.QuestionID&topicId=@Model.Topic.TopicID">
                <button>+ Answer</button>
            </a>
        </div>
    }

</div>